# vercel-test

Node.js + Express API route for Vercel.

- Install deps: `npm install`
- Local dev (requires Vercel CLI): `npm run dev`
- Deploy: `vercel deploy --prod` (or connect the repo in the Vercel dashboard)
